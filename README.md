Act as an expert UI/UX and Front-End Developer. Write clean, semantic, and fully responsive HTML5 and CSS3 code for a modern, premium Restaurant Reservation Form.

The design should look elegant and high-end, matching a luxury dining aesthetic. Use a dark or warm sophisticated color palette (e.g., deep charcoal background, clean white inputs, with gold or amber accents for focus states and buttons).

Please provide the code in two separate files: `reservation.html` and `reservation.css`.

### Form Layout & Requirements:
1. **Form Container:**
   - Centered on the page with a beautiful background (either a blurred food image overlay or a clean geometric gradient).
   - Card-style layout with smooth rounded corners, a subtle glow/box-shadow, and a modern title (e.g., "Book A Table").

2. **Form Fields (Grid Layout):**
   - **Full Name:** Text input with a placeholder.
   - **Email Address & Phone Number:** Two inputs placed side-by-side in a responsive row.
   - **Number of Guests:** A dropdown menu (Select element) ranging from 1 to 10+ guests.
   - **Date & Time:** Side-by-side inputs utilizing HTML5 type="date" and type="time".
   - **Special Requests:** A textarea for extra notes (e.g., allergies, birthdays).

3. **Interactive Visual Effects (UX):**
   - **Floating Labels or Focus Effects:** When a user clicks inside an input, the border color should smoothly change to the accent color (gold/amber) with a soft glow effect (`box-shadow`).
   - **Submit Button:** A prominent CTA button ("Confirm Reservation") that has a sleek hover effect (e.g., slight color shift, scaling, or a subtle shine transition).

### Technical Guidelines:
- Use semantic HTML5 form tags including proper `<label>` elements for web accessibility (A11y).
- Use CSS Flexbox and Grid to ensure the form scale beautifully down to mobile screens.
- Use a clean Google Font (like 'Inter' or 'Playfair Display' for the headings).
- Pure HTML and CSS only—no external CSS frameworks like Bootstrap. Include clear code comments.
